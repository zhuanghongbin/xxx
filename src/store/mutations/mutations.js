export default {
  // plus (state, param) {
  //   state.count += param.num
  //   state.product = state.product.map(item => {
  //     return {
  //       name: item.name,
  //       price: item.price + param.num
  //     }
  //   })
  // },
  // minus (state, param) {
  //   state.count -= param.num
  //   state.age = param.age
  //   state.product = state.product.map(item => {
  //     return {
  //       name: item.name,
  //       price: item.price - param.num
  //     }
  //   })
  // },
  // // 在需要在对象上添加新属性。这里只能使用 Vue.set(obj, 'newProp', 123), 或者
  // // 以新对象替换老对象。例如，利用 stage-3 的对象展开运算符我们可以这样写
  // // state.obj = { ...state.obj, newProp: 123 }
  // addParam (state) {
  //   state.person = {...state.person, sex: '男'}
  // }
}
